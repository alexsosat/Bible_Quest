import 'package:flutter/material.dart';

class AppIcons {
  static final currencyIcon = Icon(
    Icons.monetization_on,
    color: Colors.yellow,
  );

  static final healthIcon = Icon(
    Icons.favorite,
    color: Colors.red,
  );
}
